"""PySide6 user interface.

This layer never computes a domain value. It collects inputs, calls the core,
and renders results through the same formatters the written report uses, so the
screen and the report can never disagree.
"""
