# Runtime
PySide6==6.11.1

# Development only — never imported by shipped code.
# pyproj is used solely as an optional third-opinion check in the test suite;
# the suite skips those tests when it is absent.
pytest==9.1.0
