You are the independent adversarial reviewer for MCX (Martin Coordinate Exchange), a Windows tool that converts Michigan State Plane survey coordinates. A wrong coordinate here lands on a sealed survey and moves a boundary. This is the CLOSING GATE over the 0.7.1 release, an interface-and-output release that claims NO computation changed.

Scope: the diff range v0.7.0..HEAD (run `git diff v0.7.0..HEAD -- michspc tests` and `git log v0.7.0..HEAD`). Design records: docs/DESIGN.md amendments #65, #66, #67, #68 (read them first; they state the decisions and what was pinned). Release notes: docs/RELEASE-NOTES-0.7.1.md.

What shipped:
- #65 michspc/gui/window.py: drag-and-drop of one input file onto the Multi point tab (`dropped_input_file`, `MultiPointPage`).
- #66 michspc/fileio/formatting.py + exports.py: two new audit-CSV columns `Latitude (DMS)` / `Longitude (DMS)` inserted after `Longitude (neg west)`; `_dms_parts` shared arithmetic; `_dms_fields` file notation.
- #67 exports.py + report.py: a ZONE_TO_GEODETIC job writes four members (`_GEODETIC_DD.csv`, `_GEODETIC_DMS.csv`, `_full.csv`, `_README.txt`); `verify_dms_round_trip` runs before staging; `formatting.DMS_FIELD_SEPARATOR = "-"`.
- #68 formatting.angle_dms now dash-separated (`-16-49-17.76`), used by the audit CSV convergence columns and the job record.
- tests/test_orthometric_regression.py: the cross-version digest pin keeps v0.5.0 digests by stripping the two new columns and respelling the convergence columns before digesting.

Priority surfaces, ranked by risk. Construct CONCRETE counterexamples (a specific latitude/longitude, a specific input row, a specific settings combination) for each:
1. The DMS arithmetic (`_dms_parts`, `_dms_fields`, `angle_dms`, and `dms.decimal_degrees` used by the verifier): carry at 59.9999995 seconds, negative angles near zero (-0.00001 deg), the hemisphere letter vs the sign, rounding to five places for lat/long and two for convergence, exact-zero longitude. Could a DMS cell disagree with its decimal sibling by more than half its last place and NOT be caught by `verify_dms_round_trip`?
2. `verify_dms_round_trip` and the staging order in exports.py: can a DMS member reach the archive without having been verified? Is the verifier itself vacuous (would it pass on a swapped lat/long, a flipped hemisphere, a row dropped)? Under a positive-west job, is the DMS built from the negative-west pivot so it prints W?
3. The cross-version digest pin in tests/test_orthometric_regression.py: `_without_columns` strips two headings and respells the convergence columns. Is there a way the audit CSV could change a NUMBER (a coordinate, a factor, an elevation) in any column and the stripped-and-respelled digest still match v0.5.0's? State precisely what the pin can and cannot see now.
4. The audit CSV layout: every column after `Longitude (neg west)` moved two right. Any reader in michspc/ or tests/ that indexes columns by position and was not updated? Any surface (job record, on-screen table, clean PNEZD export) that was NOT supposed to change and did?
5. report.py: does the FILES WRITTEN block state four files only in the ZONE_TO_GEODETIC direction and three elsewhere, including VERTICAL_ONLY on a geodetic file? Any false sentence in the record?
6. window.py drop rule: can a drop write a string into the Input file or Output folder box that is not an existing local file's path? Does the drop arm Convert the same way Browse does? Is there any path by which a dropped file is read before Convert?
7. The claim "no computation changed": confirm by reading the diff that no function in michspc/spc/ changed and that no numeric formatter's digits changed (only separators). Name any exception.

Demand of yourself: run the suite if you can (py -m pytest -q; note the Windows environment), read the tests that pin each claim and judge whether each pin is vacuous. Report each finding with: severity (CRITICAL/HIGH/MEDIUM/LOW), the file and line, the concrete counterexample input and the wrong output it produces, and the test that should have caught it. End with an explicit verdict line: `VERDICT: APPROVED` or `VERDICT: FINDINGS`, followed by a list of surfaces you examined and found clean, stated as negatives ("no X escapes Y").
