You are the independent adversarial reviewer at the CLOSING gate for a new feature in MCX, a Michigan State Plane coordinate converter used by a licensed professional surveyor.

CORRECTNESS TIER: a wrong coordinate here lands on a sealed survey or a recorded legal description and MOVES A BOUNDARY. Calibrate severity to that.

SCOPE: `git diff v0.5.0..HEAD` - five commits, WP-E1 through WP-E5. All unreleased.

THE FEATURE: the input Z column may be declared to hold ELLIPSOID heights (GNSS) rather than orthometric heights. The program then computes H = h - N at each point from the job's geoid model.

THE OWNER'S RULES, which the implementation must obey exactly:
- The selector defaults to ORTHOMETRIC, so every pre-existing job is unchanged.
- HORIZONTAL mode NEVER converts the Z column - the height is written back exactly as supplied - but the FACTORS must still be computed from the derived orthometric height (the elevation factor is R/(R+H+N), so feeding h where H is expected adds the separation twice, ~5 ppm in Michigan).
- The two vertical modes DO write the derived elevation, datum-tagged.
- Input only; no ellipsoid output.

DESIGN AUTHORITY: docs/DESIGN.md. Read amendment #54 first - it states what was intended, what the design review already caught, and three decisions the session lead took that the owner has not yet reviewed. Then #50 (per-side geoid), #52 (the model tag), #48 (the elevations row), #41 (source-era factors), #36 (negative sigma). docs/method/METHOD.md is the standing method.

RISK-RANKED PRIORITY SURFACES, highest first:

A. `michspc/job.py` `_convert_row`: the h -> H block and the invariant it claims - "from that line down, elevation_m is an orthometric height in metres, on every path, in every mode". IS THAT ACTUALLY TRUE? Enumerate every read of elevation_m, supplied_m, height_m and written_m and prove or refute it. The design review already caught one violation of this class (the identity branch overwriting the converted height); look for others, including any introduced by the fix.

B. Can a WRONG ELEVATION reach an output? Sign, double conversion, conversion skipped, the wrong geoid model consulted, h escaping into a column labelled as an elevation, or H escaping into one labelled as an ellipsoid height. Check the interaction of ellipsoid input with: the identity branch, the modeled VERTCON branch, #41's source-era factor path, and the geoid-swap path (which is refused - verify the refusal is total and cannot be bypassed).

C. HORIZONTAL MODE BYTE-IDENTITY. Prove or refute that a job with the default ORTHOMETRIC setting produces byte-identical output to v0.5.0 on every surface. Then prove the horizontal ELLIPSOID case writes the same Z as the orthometric case and differs ONLY in the factors.

D. The three refusals in `run`, and whether any ACCEPTED configuration is wrong rather than merely refused. Build the full matrix: 3 modes x {identity, modeled both directions, swap} x {ellipsoid, orthometric} x {model on input side, output side, both, neither}.

E. `michspc/fileio/report.py` and `exports.py`: can any WRITTEN output state something FALSE about what was done to a height? The new ELLIPSOID HEIGHT CONVERSION block, the ELEVATIONS bucket branches (the review already caught one false VERTCON sentence - are there others?), the new audit columns, and whether `Source elevation + Vertical shift = Elevation` actually holds. The clean PNEZD export must stay five headerless fields.

F. `michspc/gui/*`: can the screen or the clipboard show a height under a label that is wrong for it? The height-kind control's graying, its emission into settings on both tabs, and the generalised model tag in results_model.

G. TEST QUALITY: are the new pins VACUOUS - would any pass against the defect it claims to catch? In particular `tests/fixtures/ellipsoid_height_anchors.py` claims 14 NGS published benchmarks captured live; check the fixture's own honesty statement about what it does not prove. Are any expected values read back from the code's own output rather than derived independently?

METHOD: construct CONCRETE COUNTEREXAMPLES. A finding without specific inputs and the wrong output they produce is not a finding. Rank CRITICAL / HIGH / MEDIUM / LOW. Do not report style. Do not propose refactors that change no behaviour.

END WITH AN EXPLICIT VERDICT LINE at the start of a line: either "VERDICT: APPROVED" or "VERDICT: FINDINGS" followed by the numbered findings.
